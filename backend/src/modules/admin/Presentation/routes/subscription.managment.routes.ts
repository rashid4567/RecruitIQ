// import { Router } from "express";
// import { createPlanController, getPlanByIdController, getPlanController, hidePlanController, unhidePlanController, updatePlanController } from "../containers/subscription.mangment.container";

// const router = Router();

// router.post("/", createPlanController.createPlan);
// router.get("/",getPlanController.getAll)
// router.get("/:planId",getPlanByIdController.getPlanById);
// router.put("/:planId", updatePlanController.updatedPlan);
// router.patch("/:planId/hide", hidePlanController.hidePlan);
// router.patch("/:planId/unhide", unhidePlanController.unhidePlan);


// export default router;